services:
  # n8n Workflow Automation
  n8n:
    profiles: ["extras"]
    image: n8nio/n8n:latest
    container_name: library-n8n
    restart: unless-stopped
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${N8N_PORT:-5678}:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_USER:-admin}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_PASSWORD:-changeme}
      - N8N_HOST=${N8N_HOST:-localhost}
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - N8N_SECURE_COOKIE=false
      - NODE_ENV=production
      - WEBHOOK_URL=http://${N8N_HOST:-localhost}:5678/
      - GENERIC_TIMEZONE=${TIMEZONE:-Asia/Ho_Chi_Minh}
      - EXECUTIONS_PROCESS=main
      - EXECUTIONS_MODE=regular
      - LIBRARY_API_URL=${LIBRARY_API_URL:-http://api:8000}
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - DSPACE_API_URL=${DSPACE_API_URL:-https://lib.hpu.edu.vn/server}
      - DSPACE_API_TOKEN=${DSPACE_API_TOKEN}
      - DSPACE_BASE_URL=${DSPACE_BASE_URL:-https://lib.hpu.edu.vn}
      - DSPACE_DEFAULT_COLLECTION=${DSPACE_DEFAULT_COLLECTION}
      - ADMIN_EMAIL=${ADMIN_EMAIL:-library-admin@hpu.edu.vn}
    volumes:
      - n8n_data:/home/node/.n8n
      - ./workflows:/workflows
      - ./scripts:/opt/library-digitization/scripts
      - ${DIGITIZE_DATA_DIR}:/data/digitization
    networks:
      - library-net
    depends_on:
      - postgres
      - redis

  # PostgreSQL Database
  postgres:
    # profiles: ["extras"]
    image: postgres:15-alpine
    container_name: library-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-library_digitization}
      - POSTGRES_USER=${POSTGRES_USER:-libraryuser}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
      - POSTGRES_INITDB_ARGS=--encoding=UTF-8 --locale=en_US.UTF-8
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - library-net
    ports:
      # KHÔNG mở PostgreSQL ra ngoài: chỉ loopback, và đổi cổng để không đâm vào PostgreSQL
      # có thể đã chạy sẵn trên máy chủ. Trong mạng Docker các service vẫn dùng postgres:5432.
      - "${BIND_ADDR:-127.0.0.1}:${POSTGRES_HOST_PORT:-5433}:5432"

  # Redis for Queue Management
  redis:
    image: redis:7-alpine
    container_name: library-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - library-net
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${REDIS_HOST_PORT:-6380}:6379"

  # Processing Worker (Python with GPU support)
  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile.worker
    restart: unless-stopped
    environment:
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - POSTGRES_HOST=postgres
      - POSTGRES_DB=${POSTGRES_DB:-library_digitization}
      - POSTGRES_USER=${POSTGRES_USER:-libraryuser}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
      - DIGITIZE_DATA_DIR=/data/digitization/jobs
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      # Model provider (YC-MP-04) — dùng khi worker trích xuất qua factory (GĐ1).
      # Mặc định 'cloud' giữ nguyên hành vi hiện tại. Danh sách công cụ: scripts/providers/registry.py
      - MODEL_PROVIDER=${MODEL_PROVIDER:-cloud}
      # Công cụ đảm nhiệm mỗi chế độ khi định tuyến theo độ nhạy cảm (YC-DR).
      # Tài liệu Nội bộ/Nhạy cảm LUÔN đi vào LOCAL_PROVIDER (ràng buộc cứng YC-DR-03).
      - CLOUD_PROVIDER=${CLOUD_PROVIDER:-claude}
      - LOCAL_PROVIDER=${LOCAL_PROVIDER:-ollama}
      # -- Công cụ TẠI CHỖ: điểm cuối là tên service trong mạng nội bộ library-net --
      - OLLAMA_URL=http://ollama:11434
      - LOCAL_MODEL=${LOCAL_MODEL:-qwen2.5:7b}
      - OLLAMA_EMBED_MODEL=${OLLAMA_EMBED_MODEL:-}
      - VLLM_BASE_URL=${VLLM_BASE_URL:-http://vllm:8000/v1}
      - VLLM_MODEL=${VLLM_MODEL:-}
      - LLAMACPP_BASE_URL=${LLAMACPP_BASE_URL:-http://llamacpp:8080/v1}
      - LLAMACPP_MODEL=${LLAMACPP_MODEL:-local-model}
      # -- Công cụ ĐÁM MÂY: chỉ có hiệu lực khi được chọn; khóa nào trống thì công cụ đó không dùng được --
      - CLOUD_MODEL=${CLOUD_MODEL:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENAI_MODEL=${OPENAI_MODEL:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GEMINI_MODEL=${GEMINI_MODEL:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY:-}
      - TOGETHER_API_KEY=${TOGETHER_API_KEY:-}
      - MOONSHOT_API_KEY=${MOONSHOT_API_KEY:-}
      - MOONSHOT_MODEL=${MOONSHOT_MODEL:-}
      - MOONSHOT_BASE_URL=${MOONSHOT_BASE_URL:-}
      - DASHSCOPE_API_KEY=${DASHSCOPE_API_KEY:-}
      - DASHSCOPE_MODEL=${DASHSCOPE_MODEL:-}
      - DASHSCOPE_BASE_URL=${DASHSCOPE_BASE_URL:-}
      - AZURE_OPENAI_BASE_URL=${AZURE_OPENAI_BASE_URL:-}
      - AZURE_OPENAI_API_KEY=${AZURE_OPENAI_API_KEY:-}
      - AZURE_OPENAI_MODEL=${AZURE_OPENAI_MODEL:-}
      - OPENAI_COMPAT_BASE_URL=${OPENAI_COMPAT_BASE_URL:-}
      - OPENAI_COMPAT_MODEL=${OPENAI_COMPAT_MODEL:-}
      - OPENAI_COMPAT_API_KEY=${OPENAI_COMPAT_API_KEY:-}
      - OPENAI_COMPAT_DEPLOYMENT=${OPENAI_COMPAT_DEPLOYMENT:-cloud}
      # -- Pipeline trích xuất qua lớp provider (ADR-008) --
      # USE_PROVIDER_LAYER=0 là van lùi về đường cũ, không cần build lại image
      - USE_PROVIDER_LAYER=${USE_PROVIDER_LAYER:-1}
      # Dự phòng chỉ trong cùng chế độ — KHÔNG bao giờ tại chỗ → đám mây
      - LOCAL_FALLBACK_PROVIDERS=${LOCAL_FALLBACK_PROVIDERS:-}
      - CLOUD_FALLBACK_PROVIDERS=${CLOUD_FALLBACK_PROVIDERS:-}
      - LOW_CONFIDENCE_THRESHOLD=${LOW_CONFIDENCE_THRESHOLD:-0.5}
      - CONTEXT_FULL_MAX_PAGES=${CONTEXT_FULL_MAX_PAGES:-30}
      - CONTEXT_FULL_MAX_CHARS=${CONTEXT_FULL_MAX_CHARS:-20000}
      - METRICS_GPU=${METRICS_GPU:-0}
      - ALLOW_PUBLIC_LOCAL_ENDPOINT=${ALLOW_PUBLIC_LOCAL_ENDPOINT:-}
    volumes:
      - ./scripts:/app/scripts
      - ./configs:/app/configs
      - ${DIGITIZE_DATA_DIR}:/data/digitization
    networks:
      - library-net
    depends_on:
      - redis
    # TÀI NGUYÊN: đặt qua .env để KHỚP VỚI MÁY CHỦ THẬT, không cứng trong file.
    # Giá trị cũ (2 worker × 10 CPU giới hạn / 8 CPU dự trữ = 20/16 CPU) làm Docker TỪ CHỐI khởi động
    # trên máy 4 CPU: "Range of CPUs is from 0.01 to 4.00".
    #
    # Mặc định dưới đây an toàn cho máy 4 CPU / 8GB: 1 worker, giới hạn 2 CPU, KHÔNG dự trữ cứng.
    # Vì sao bỏ `reservations`: Docker phải giữ sẵn phần CPU đó, nên đặt cao là cách nhanh nhất làm
    # deploy fail; OCR chạy theo đợt nên giới hạn trần là đủ.
    #   Máy 8 CPU/16GB:  WORKER_REPLICAS=2  WORKER_CPUS=3  WORKER_MEMORY=6G
    #   Máy 16 CPU/32GB: WORKER_REPLICAS=3  WORKER_CPUS=4  WORKER_MEMORY=8G
    # Quy tắc: WORKER_REPLICAS × WORKER_CPUS phải NHỎ HƠN tổng số CPU (chừa cho api/ui/postgres).
    deploy:
      replicas: ${WORKER_REPLICAS:-1}
      resources:
        limits:
          cpus: "${WORKER_CPUS:-2}"
          memory: "${WORKER_MEMORY:-3G}"

  # ===================================================================
  # MODEL SERVING TẠI CHỖ (YC-MS) — BA lựa chọn, mỗi lựa chọn một profile RIÊNG.
  # `docker compose up` mặc định KHÔNG chạy cái nào (bổ sung, không viết lại).
  # Chỉ bật MỘT công cụ tại một thời điểm để không tranh RAM/GPU:
  #     Ollama    (dựng nhanh nhất, CPU tốt):  docker compose --profile local-ai up -d
  #     vLLM      (thông lượng cao, cần GPU):  docker compose --profile local-ai-vllm up -d
  #     llama.cpp (nhẹ nhất, CPU):             docker compose --profile local-ai-llamacpp up -d
  # Rồi đặt LOCAL_PROVIDER=ollama | vllm | llamacpp trong .env. KHÔNG service nào map cổng ra host
  # (YC-MS-01, YC-BM-05) — chỉ truy cập được trong mạng nội bộ library-net.
  # ===================================================================

  # Lựa chọn 1 — Ollama (ADR-002: công cụ của GĐ0)
  #   Tải model 1 lần:  docker compose exec ollama ollama pull qwen2.5:7b
  #   Kiểm chứng ngắt mạng (KT-BM-01): sau khi có model trong volume, ngắt Internet vẫn xử lý được.
  ollama:
    profiles: ["local-ai"]
    image: ollama/ollama:latest
    container_name: library-ollama
    restart: unless-stopped
    # KHÔNG map cổng ra host (YC-MS-01, YC-BM-05): chỉ truy cập trong mạng nội bộ library-net.
    expose:
      - "11434"
    volumes:
      # YC-MS-02: model lưu trên volume, không tải lại mỗi lần khởi động
      - ollama_data:/root/.ollama
    networks:
      - library-net
    healthcheck:
      # YC-MS-04: kiểm tra sẵn sàng trước khi đưa tài liệu vào xử lý
      test: ["CMD-SHELL", "ollama list >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    # GPU tùy chọn: nếu server có NVIDIA GPU, bỏ comment khối dưới (SRS: không GPU thì chạy CPU).
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]

  # Lựa chọn 2 — vLLM: thông lượng cao khi có GPU. Ứng viên thay Ollama ở GĐ1 nếu nghẽn (ADR-002).
  # Nói giao thức tương thích OpenAI → dùng OpenAICompatProvider, KHÔNG cần viết thêm lớp nào.
  #   Bật:  docker compose --profile local-ai-vllm up -d   +   LOCAL_PROVIDER=vllm
  # ⚠️ vLLM TẢI MODEL TỪ HUGGING FACE lúc khởi động → lần đầu PHẢI có mạng; sau đó model nằm trong
  #    volume nên chạy được khi ngắt Internet (YC-MS-02/03). Rà giấy phép model TRƯỚC (YC-PL-01).
  vllm:
    profiles: ["local-ai-vllm"]
    image: vllm/vllm-openai:latest
    container_name: library-vllm
    restart: unless-stopped
    expose:
      - "8000"
    command: >
      --model ${VLLM_MODEL:-Qwen/Qwen2.5-7B-Instruct}
      --max-model-len ${VLLM_MAX_MODEL_LEN:-8192}
      --gpu-memory-utilization ${VLLM_GPU_MEM_UTIL:-0.90}
    environment:
      - HF_HOME=/models
      # Chỉ cần khi tải model có kiểm soát truy cập (gated) — vd họ Llama
      - HUGGING_FACE_HUB_TOKEN=${HUGGING_FACE_HUB_TOKEN:-}
    volumes:
      - vllm_models:/models      # YC-MS-02: model bền vững qua khởi động lại
    networks:
      - library-net
    healthcheck:
      # YC-MS-04: /health là điểm cuối kiểm tra sẵn sàng của vLLM
      test: ["CMD-SHELL", "python -c \"import urllib.request;urllib.request.urlopen('http://localhost:8000/health')\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 300s         # nạp model lần đầu có thể mất vài phút
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # Lựa chọn 3 — llama.cpp: nhẹ nhất, chạy CPU, không cần GPU. Dùng model GGUF đặt sẵn trong volume
  # → phù hợp nhất cho kịch bản air-gapped tuyệt đối (không tải gì lúc khởi động).
  #   Đặt file .gguf vào volume llamacpp_models rồi:
  #   docker compose --profile local-ai-llamacpp up -d   +   LOCAL_PROVIDER=llamacpp
  llamacpp:
    profiles: ["local-ai-llamacpp"]
    image: ghcr.io/ggml-org/llama.cpp:server
    container_name: library-llamacpp
    restart: unless-stopped
    expose:
      - "8080"
    command: >
      -m /models/${LLAMACPP_GGUF_FILE:-model.gguf}
      --host 0.0.0.0 --port 8080
      -c ${LLAMACPP_CTX_SIZE:-8192}
      -t ${LLAMACPP_THREADS:-8}
    volumes:
      - llamacpp_models:/models   # YC-MS-02: đặt file .gguf đã rà giấy phép vào đây
    networks:
      - library-net
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # FastAPI Service - Enhanced API with UI support
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    container_name: library-api
    restart: unless-stopped
    ports:
      # Caddy (network_mode: host) gọi được qua localhost; không phơi trực tiếp ra Internet
      - "${BIND_ADDR:-127.0.0.1}:${API_PORT:-8000}:8000"
    environment:
      - DIGITIZE_DATA_DIR=/data/digitization/jobs
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      - POSTGRES_HOST=postgres
      - POSTGRES_DB=${POSTGRES_DB:-library_digitization}
      - POSTGRES_USER=${POSTGRES_USER:-libraryuser}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
      - REDIS_HOST=redis
      - DSPACE_API_URL=${DSPACE_API_URL}
      - DSPACE_API_TOKEN=${DSPACE_API_TOKEN}
      # ⚠️ PHẢI GIỮ ĐỒNG BỘ với service `worker`: endpoint /api/v2/providers đọc cấu hình từ CHÍNH
      # container này để hiển thị trên trang quản trị (YC-MS-08). Lệch nhau thì trang quản trị sẽ
      # báo công cụ khác với công cụ worker đang thật sự dùng — sai lệch nguy hiểm hơn là không có.
      - MODEL_PROVIDER=${MODEL_PROVIDER:-cloud}
      - CLOUD_PROVIDER=${CLOUD_PROVIDER:-claude}
      - LOCAL_PROVIDER=${LOCAL_PROVIDER:-ollama}
      - OLLAMA_URL=http://ollama:11434
      - LOCAL_MODEL=${LOCAL_MODEL:-qwen2.5:7b}
      - VLLM_BASE_URL=${VLLM_BASE_URL:-http://vllm:8000/v1}
      - VLLM_MODEL=${VLLM_MODEL:-}
      - LLAMACPP_BASE_URL=${LLAMACPP_BASE_URL:-http://llamacpp:8080/v1}
      - LLAMACPP_MODEL=${LLAMACPP_MODEL:-local-model}
      - CLOUD_MODEL=${CLOUD_MODEL:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY:-}
      - TOGETHER_API_KEY=${TOGETHER_API_KEY:-}
      - MOONSHOT_API_KEY=${MOONSHOT_API_KEY:-}
      - DASHSCOPE_API_KEY=${DASHSCOPE_API_KEY:-}
      - AZURE_OPENAI_BASE_URL=${AZURE_OPENAI_BASE_URL:-}
      - AZURE_OPENAI_API_KEY=${AZURE_OPENAI_API_KEY:-}
      - AZURE_OPENAI_MODEL=${AZURE_OPENAI_MODEL:-}
      - OPENAI_COMPAT_BASE_URL=${OPENAI_COMPAT_BASE_URL:-}
      - OPENAI_COMPAT_MODEL=${OPENAI_COMPAT_MODEL:-}
      - OPENAI_COMPAT_API_KEY=${OPENAI_COMPAT_API_KEY:-}
      - OPENAI_COMPAT_DEPLOYMENT=${OPENAI_COMPAT_DEPLOYMENT:-cloud}
      - USE_PROVIDER_LAYER=${USE_PROVIDER_LAYER:-1}
      - LOCAL_FALLBACK_PROVIDERS=${LOCAL_FALLBACK_PROVIDERS:-}
      - CLOUD_FALLBACK_PROVIDERS=${CLOUD_FALLBACK_PROVIDERS:-}
      - ALLOW_PUBLIC_LOCAL_ENDPOINT=${ALLOW_PUBLIC_LOCAL_ENDPOINT:-}
    volumes:
      - ./api:/app/api
      - ./scripts:/app/scripts
      - ./configs:/app/configs
      - ${DIGITIZE_DATA_DIR}:/data/digitization
    networks:
      - library-net
    depends_on:
      - redis
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # UI Service - Digitization Dashboard
  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
      # NEXT_PUBLIC_* PHẢI truyền lúc BUILD (xem ui/Dockerfile). PUBLIC_BASE_URL là URL mà TRÌNH
      # DUYỆT dùng — qua Caddy, vd https://sohoa.hpu.edu.vn. Đổi giá trị này thì phải build lại UI:
      #   docker compose up -d --build ui
      args:
        NEXT_PUBLIC_OCR_API_URL: ${PUBLIC_API_URL:-http://localhost:8000}
        NEXT_PUBLIC_DSPACE_URL: ${DSPACE_BASE_URL:-https://lib.hpu.edu.vn}
        # Chỉ là URL công khai để hiển thị. KHÔNG phải đường server tự gọi chính nó
        # (cái đó là SITE_INTERNAL_URL ở phần environment bên dưới).
        NEXT_PUBLIC_SITE_URL: ${PUBLIC_BASE_URL:-http://localhost:3200}
    container_name: library-ui
    restart: unless-stopped
    ports:
      # ⚠️ KHÔNG dùng 3000: chat.hpu.edu.vn đã chiếm cổng đó trên máy chủ trung tâm
      - "${BIND_ADDR:-127.0.0.1}:${UI_PORT:-3200}:3000"
    environment:
      # DSpace config
      - NEXT_PUBLIC_DSPACE_URL=${DSPACE_BASE_URL}
      
      # OCR API config
      # Giá trị này chỉ có tác dụng phía SERVER (bản nhúng vào client đã cố định lúc build ở trên).
      # Đặt đúng URL công khai để hai bên nhất quán.
      - NEXT_PUBLIC_OCR_API_URL=${PUBLIC_API_URL:-http://localhost:8000}
      # Đường gọi TỪ SERVER (server component / route handler) — đi mạng nội bộ Docker, nhanh hơn
      # và không phụ thuộc DNS/TLS bên ngoài.
      - OCR_API_INTERNAL_URL=http://api:8000
      # Địa chỉ để server component TỰ GỌI route handler của chính nó (kiểm tra phiên DSpace).
      # PHẢI là loopback CỦA CHÍNH CONTAINER — dùng URL công khai sẽ bắt lời gọi đi ra Internet rồi
      # quay lại, Docker chặn đường vòng đó và người dùng bị kẹt ở form đăng nhập.
      - SITE_INTERNAL_URL=http://127.0.0.1:3000
      
      # Claude API (for metadata extraction)
      - CLAUDE_API_KEY=${CLAUDE_API_KEY}
      
      # Node environment
      - NODE_ENV=production
    networks:
      - library-net
    depends_on:
      - api
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Monitoring Dashboard (Optional - Grafana)
  grafana:
    profiles: ["extras"]
    image: grafana/grafana:latest
    container_name: library-grafana
    restart: unless-stopped
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${GRAFANA_PORT:-3201}:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-changeme}
      - GF_INSTALL_PLUGINS=grafana-clock-panel,grafana-simple-json-datasource
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources
    networks:
      - library-net
    depends_on:
      - postgres

  # File Browser (Optional - for managing processed files)
  filebrowser:
    # Trình quản lý file TÙY CHỌN — trước đây chạy mặc định và chiếm cổng 8080 của máy chủ.
    # Bật khi cần:  docker compose --profile extras up -d filebrowser
    profiles: ["extras"]
    image: filebrowser/filebrowser:latest
    container_name: library-filebrowser
    restart: unless-stopped
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${FILEBROWSER_PORT:-8081}:80"
    volumes:
      - ${DIGITIZE_DATA_DIR}:/srv
      - filebrowser_data:/database
    environment:
      - FB_DATABASE=/database/filebrowser.db
      - FB_ROOT=/srv
    networks:
      - library-net

volumes:
  ollama_data:          # YC-MS-02: lưu model tại chỗ, bền vững qua khởi động lại
    driver: local
  vllm_models:          # YC-MS-02: cache model của vLLM (HF_HOME)
    driver: local
  llamacpp_models:      # YC-MS-02: chứa file .gguf cho llama.cpp
    driver: local
  n8n_data:
    driver: local
  postgres_data:
    driver: local
  redis_data:
    driver: local
  grafana_data:
    driver: local
  filebrowser_data:
    driver: local

networks:
  library-net:
    driver: bridge